<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hae5.sfaas.user.mapper.UserMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO USER (factory_id, name, employee_id, password, dept_id, position, role)
        VALUES (#{factoryId}, #{name}, #{employeeId}, #{password}, #{departmentId}, #{position}, #{role});
    </insert>

    <delete id="deleteAll">
        DELETE FROM USER;
    </delete>


    <select id="findById" resultType="com.hae5.sfaas.user.model.User">
        SELECT u.*
        FROM USER as u
        WHERE u.user_id = #{userId}
        LIMIT 1;
    </select>

    <select id="findAll" resultType="com.hae5.sfaas.user.model.User">
        SELECT u.*
        FROM USER as u;
    </select>

    <select id="findByEmployeeId" resultType="com.hae5.sfaas.user.model.User">
        SELECT u.*
        FROM USER as u
        WHERE u.employee_id = #{employeeId}
        LIMIT 1;
    </select>

</mapper>